# Spring Boot demos

Place Spring Boot services here. Each demo can be a separate project folder so you can run
and deploy them independently.

Suggested layout:
- `demo-service/` for your first REST API
- `shared/` for shared DTOs or configs
